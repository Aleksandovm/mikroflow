services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mikroflow
      POSTGRES_PASSWORD: mikroflow
      POSTGRES_DB: mikroflow
    # Published only on the server's loopback — reach it from your laptop via an
    # SSH tunnel, never expose 5432 to the network directly.
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mikroflow"]
      interval: 5s
      timeout: 3s
      retries: 10

  collector:
    build: .
    command: python -m mikroflow.collector.main
    env_file: .env
    environment:
      MIKROFLOW_DB_DSN: postgresql://mikroflow:mikroflow@postgres:5432/mikroflow
    ports:
      - "2055:2055/udp"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  worker:
    build: .
    command: python -m mikroflow.worker.main
    env_file: .env
    environment:
      MIKROFLOW_DB_DSN: postgresql://mikroflow:mikroflow@postgres:5432/mikroflow
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
